# python_au
class Solution:
    def reverseList(self, head: ListNode) -> ListNode:
        prev = None
        cur = head
        while cur != None:
            nextc = cur.next
            cur.next = prev
            prev = cur
            cur = nextc
        return prev
